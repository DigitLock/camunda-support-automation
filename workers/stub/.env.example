# Copy to .env and fill in. The worker reads exactly these three variables (design §7).
# The SDK appends /v2 to the base URL itself.
CAMUNDA_BASE_URL=http://localhost:8080
CAMUNDA_USER=admin
CAMUNDA_PASSWORD=REPLACE_ME
